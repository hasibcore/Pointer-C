#include<stdio.h>
#include<string.h>
int main ()
{
    int n,i;
     printf("Enter the size of String : ");
     scanf("%d",&n);
     char str[n];
    printf("Enter the array elements : ");
   char *p=str;
gets(p);
    puts(p);

}
